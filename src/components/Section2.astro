<div class="container">
  <div class="content">
    <div class="left-section"></div>
    <div class="right-section">
      <div class="text">Texto de ejemplo</div>
      <a href="#" class="btn-example">Enviar</a>
    </div>
  </div>
</div>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    overflow-x: hidden;
  }

  .container {
    background-color: #f6b408;
    width: 100vw;
    margin-left: calc(-50vw + 50%);
    margin-right: calc(100% - 2rem);
    margin-top: 1rem;
    margin-bottom: 1rem;
    padding: 3rem;
    justify-content: center;
    align-items: center;
  }

  .content {
    display: grid;
    grid-template-columns: 1fr auto; /* 1fr para el contenido y auto para el rectángulo */
    grid-template-rows: auto auto; /* Dos filas: una para el texto y otra para el botón */
    grid-template-areas:
      "left right"
      "left right";
    gap: 10px; /* Espacio entre las secciones */
    align-items: stretch; /* Asegura que ambas columnas tengan la misma altura */
    width: 100%; /* Asegura que el contenedor ocupe todo el ancho de la pantalla */
  }

  .right-section {
    display: flex;
    flex-direction: column; /* Alinea el cuadro de texto y el botón uno debajo del otro */
    grid-area: right;
    width: 100%;
  }

  .text {
    padding: 10px;
    font-size: 16px;
    width: 100%; /* Ocupar todo el ancho disponible */
  }

  .btn-example {
    padding: 10px;
    font-size: 16px;
    margin-top: 10px;
    text-align: center;
    text-decoration: none;
    background-color: #d13582;
    border-radius: 1.5rem;
    color: white;
    cursor: pointer;
    display: inline-block;
    width: 100%; /* Ocupa todo el ancho disponible */
  }

  .left-section {
    background-color: #ffcc4d; /* Color del rectángulo */
    width: 50%; /* Ancho fijo del rectángulo */
    grid-area: left;
    height: auto;
  }

  /* Media query para pantallas pequeñas */
  @media (max-width: 768px) {
    .content {
      grid-template-columns: 1fr; /* Cambia a una sola columna en pantallas pequeñas */
      grid-template-rows: auto auto auto; /* Tres filas: una para cada elemento */
      grid-template-areas:
        "right"
        "left"
        "left"; /* El rectángulo se coloca arriba */
    }

    .right-section {
      width: 100%; /* El rectángulo ocupará el ancho completo */
      height: 50px; /* Ajusta la altura según sea necesario */
    }

    .left-section {
      width: 100%; /* La sección izquierda también ocupa todo el ancho */
    }
  }
</style>
